import React from "react";
import SearchForm from "./SearchForm";
import MovieList from "./MovieList";
const Home = () => {
  return (
    <main>
      <SearchForm />
      <MovieList />
    </main>
  );
};

export default Home;
